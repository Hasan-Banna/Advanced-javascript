let bonus = 20;

function sum (num1, num2){
    let result = num1 + num2 + bonus;
    //console.log(result);
    if(result > 9){
        var mood = 'happy'; //let,const dile error ashbe but var dile error ashbe na.
        mood = 'fishy';
        mood = 'funky';
        // console.log(mood); // if er baire hole error hobe.
    }
console.log(day); //var diye kore tar opor initialize korle undefined hobe let,const hole error ashbe.
let day = "friday"; // let,const dile error ashbe

    return result;
}
  const output = sum(3, 7);
//  console.log(output);